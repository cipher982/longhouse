# Sanitized tool-parity mini-fixtures

Synthetic provider transcript snippets for parser regression docs.
Native tests own the authoritative assertions in `engine/src/pipeline/parser.rs`
and `server/tests_lite/test_cursor_transcript.py`.
