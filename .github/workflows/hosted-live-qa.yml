name: Hosted Live QA

on:
  schedule:
    - cron: "41 10 * * *"
  workflow_call:
    inputs:
      source_sha:
        description: "Exact QA checkout commit"
        required: true
        type: string
      runtime_image_tag:
        description: "Runtime image tag selected by the deployment"
        required: true
        type: string
  workflow_dispatch:
    inputs:
      source_sha:
        description: "Commit SHA that triggered this async QA run"
        required: false
        type: string
      runtime_image_tag:
        description: "Image tag; defaults to source_sha or the observed clean build"
        required: false
        type: string

permissions:
  contents: read

concurrency:
  group: hosted-live-qa-${{ github.repository }}
  cancel-in-progress: true

jobs:
  hosted-live-qa:
    name: "hosted-live-qa"
    runs-on: cube-deploy
    if: github.repository == 'cipher982/longhouse'
    timeout-minutes: 25
    env:
      CONTROL_PLANE_URL: ${{ vars.HOSTED_CANARY_CONTROL_PLANE_URL }}
      CONTROL_PLANE_ADMIN_TOKEN: ${{ secrets.HOSTED_CANARY_CONTROL_PLANE_ADMIN_TOKEN }}
      SMOKE_RUNTIME_TOKEN: ${{ secrets.HOSTED_CANARY_RUNTIME_TOKEN }}
      LONGHOUSE_MACHINE_TOKEN: ${{ secrets.HOSTED_CANARY_RUNTIME_TOKEN }}

      QA_INSTANCE_SUBDOMAIN: ${{ vars.HOSTED_CANARY_SUBDOMAIN }}
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          ref: ${{ inputs.source_sha || github.sha }}

      - name: Validate isolated canary configuration
        run: |
          set -euo pipefail
          case "${QA_INSTANCE_SUBDOMAIN,,}" in
            ""|david010|demo|dogfood|personal)
              echo "::error::Configure a dedicated HOSTED_CANARY_SUBDOMAIN; refusing personal dogfood or demo." >&2
              exit 1
              ;;
          esac
          for name in CONTROL_PLANE_URL CONTROL_PLANE_ADMIN_TOKEN SMOKE_RUNTIME_TOKEN LONGHOUSE_MACHINE_TOKEN; do
            if [[ -z "${!name}" ]]; then
              echo "::error::Missing required canary configuration: $name" >&2
              exit 1
            fi
          done

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: "3.12"

      - name: Prepare credential-free isolation image
        id: prepare
        run: |
          set -euo pipefail
          echo "image=$(python3 scripts/qa/test-isolation.py --prepare)" >> "$GITHUB_OUTPUT"

      - name: Verify deployed source before QA
        id: runtime_before
        env:
          EXPECTED_RUNTIME_TAG: ${{ inputs.runtime_image_tag || inputs.source_sha || '' }}
        run: |
          set -euo pipefail
          if [[ -n "$EXPECTED_RUNTIME_TAG" && ! "$EXPECTED_RUNTIME_TAG" =~ ^[0-9a-f]{40}$ ]]; then
            EXPECTED_RUNTIME_TAG="$(docker buildx imagetools inspect \
              "ghcr.io/${{ github.repository_owner }}/longhouse-runtime:$EXPECTED_RUNTIME_TAG" \
              --format '{{json .Image}}' |
              jq --exit-status --raw-output '.config.Labels["org.opencontainers.image.revision"] | select(type == "string" and test("^[0-9a-f]{40}$"))')"
          fi
          health="$(curl --fail --silent --show-error --max-time 15 "https://${QA_INSTANCE_SUBDOMAIN}.longhouse.ai/api/health")"
          printf '%s\n' "$health" | jq '{build}'
          actual_sha="$(printf '%s\n' "$health" | jq --exit-status --raw-output '.build | select(.dirty == false) | .commit | select(type == "string" and test("^[0-9a-f]{40}$"))')"
          if [[ -n "$EXPECTED_RUNTIME_TAG" && "$actual_sha" != "$EXPECTED_RUNTIME_TAG" ]]; then
            echo "Deployed runtime $actual_sha does not match requested image $EXPECTED_RUNTIME_TAG" >&2
            exit 1
          fi
          printf 'runtime_sha=%s\n' "$actual_sha" >> "$GITHUB_OUTPUT"

      - name: Run hosted live QA
        id: qa_live
        run: |
          set -euo pipefail
          credentials="$(mktemp "$RUNNER_TEMP/hosted-live-credentials.XXXXXX.json")"
          trap 'rm -f "$credentials"' EXIT INT TERM
          chmod 600 "$credentials"
          python3 - "$credentials" <<'PY'
          import json
          import os
          import sys

          keys = (
              "CONTROL_PLANE_ADMIN_TOKEN",
              "SMOKE_RUNTIME_TOKEN",
              "LONGHOUSE_MACHINE_TOKEN",
          )
          with open(sys.argv[1], "w", encoding="utf-8") as stream:
              json.dump(
                  {key: os.environ[key] for key in keys if os.environ.get(key)},
                  stream,
              )
          PY
          attempts=0
          until python3 scripts/qa/test-isolation.py \
            --live \
            --image "${{ steps.prepare.outputs.image }}" \
            --credentials "$credentials" \
            --target qa-live \
            --set "CONTROL_PLANE_URL=$CONTROL_PLANE_URL" \
            --set "QA_INSTANCE_SUBDOMAIN=$QA_INSTANCE_SUBDOMAIN" \
            --set "LONGHOUSE_API_URL=https://${QA_INSTANCE_SUBDOMAIN}.longhouse.ai" \
            --set "PLAYWRIGHT_BASE_URL=https://${QA_INSTANCE_SUBDOMAIN}.longhouse.ai" \
            --set "PLAYWRIGHT_API_BASE_URL=https://${QA_INSTANCE_SUBDOMAIN}.longhouse.ai"; do
            attempts=$((attempts + 1))
            if [[ "$attempts" -ge 2 ]]; then
              exit 1
            fi
            echo "qa-live failed; retrying once after 5s..." >&2
            sleep 5
          done

      - name: Run hosted mixed live/archive shipper bench
        id: hosted_shipper_bench
        if: always() && steps.qa_live.outcome == 'success' && env.SMOKE_RUNTIME_TOKEN != ''
        env:
          CARGO_PROFILE: ci
        run: |
          set -euo pipefail
          credentials="$(mktemp "$RUNNER_TEMP/hosted-bench-credentials.XXXXXX.json")"
          trap 'rm -f "$credentials"' EXIT INT TERM
          chmod 600 "$credentials"
          python3 - "$credentials" <<'PY'
          import json
          import os
          import sys

          keys = (
              "CONTROL_PLANE_ADMIN_TOKEN",
              "SMOKE_RUNTIME_TOKEN",
          )
          with open(sys.argv[1], "w", encoding="utf-8") as stream:
              json.dump(
                  {key: os.environ[key] for key in keys if os.environ.get(key)},
                  stream,
              )
          PY
          python3 scripts/qa/test-isolation.py \
            --live \
            --image "${{ steps.prepare.outputs.image }}" \
            --credentials "$credentials" \
            --target hosted-shipper-mixed-bench \
            --set "CONTROL_PLANE_URL=$CONTROL_PLANE_URL" \
            --set "QA_INSTANCE_SUBDOMAIN=$QA_INSTANCE_SUBDOMAIN" \
            --set "LONGHOUSE_API_URL=https://${QA_INSTANCE_SUBDOMAIN}.longhouse.ai" \
            --set "PLAYWRIGHT_API_BASE_URL=https://${QA_INSTANCE_SUBDOMAIN}.longhouse.ai"

      - name: Run render-canary SLA check
        id: render_canary
        if: always() && steps.qa_live.outcome == 'success' && env.SMOKE_RUNTIME_TOKEN != ''
        env:
          LONGHOUSE_CANARY_TOKEN: ${{ secrets.HOSTED_CANARY_TELEMETRY_TOKEN }}
        run: |
          set -euo pipefail
          if [[ -z "$LONGHOUSE_CANARY_TOKEN" ]]; then
            echo "LONGHOUSE_CANARY_TOKEN secret not set; skipping render-canary." >&2
            exit 0
          fi
          credentials="$(mktemp "$RUNNER_TEMP/render-canary-credentials.XXXXXX.json")"
          trap 'rm -f "$credentials"' EXIT INT TERM
          chmod 600 "$credentials"
          python3 - "$credentials" <<'PY'
          import json
          import os
          import sys

          keys = (
              "SMOKE_RUNTIME_TOKEN",
              "LONGHOUSE_CANARY_TOKEN",
          )
          with open(sys.argv[1], "w", encoding="utf-8") as stream:
              json.dump(
                  {key: os.environ[key] for key in keys if os.environ.get(key)},
                  stream,
              )
          PY
          python3 scripts/qa/test-isolation.py \
            --live \
            --image "${{ steps.prepare.outputs.image }}" \
            --credentials "$credentials" \
            --target render-canary \
            --set "QA_INSTANCE_SUBDOMAIN=$QA_INSTANCE_SUBDOMAIN" \
            --set "LONGHOUSE_API_URL=https://${QA_INSTANCE_SUBDOMAIN}.longhouse.ai" \
            --set "PLAYWRIGHT_BASE_URL=https://${QA_INSTANCE_SUBDOMAIN}.longhouse.ai" \
            --set "PLAYWRIGHT_API_BASE_URL=https://${QA_INSTANCE_SUBDOMAIN}.longhouse.ai"

      - name: Validate cohort journey contracts
        id: cohort_contracts
        if: always() && steps.qa_live.outcome == 'success'
        run: python3 scripts/qa/test-isolation.py --target validate-cohort-journey

      - name: Run privacy-safe isolated cohort journey
        id: cohort_journey
        if: always() && steps.qa_live.outcome == 'success'
        run: |
          set -euo pipefail
          credentials="$(mktemp "$RUNNER_TEMP/cohort-journey-credentials.XXXXXX.json")"
          trap 'rm -f "$credentials"' EXIT INT TERM
          chmod 600 "$credentials"
          python3 - "$credentials" <<'PY'
          import json
          import os
          import sys

          keys = (
              "SMOKE_RUNTIME_TOKEN",
          )
          with open(sys.argv[1], "w", encoding="utf-8") as stream:
              json.dump(
                  {key: os.environ[key] for key in keys if os.environ.get(key)},
                  stream,
              )
          PY
          python3 scripts/qa/test-isolation.py \
            --live \
            --image "${{ steps.prepare.outputs.image }}" \
            --credentials "$credentials" \
            --target cohort-journey \
            --set "QA_INSTANCE_SUBDOMAIN=$QA_INSTANCE_SUBDOMAIN" \
            --set "LONGHOUSE_API_URL=https://${QA_INSTANCE_SUBDOMAIN}.longhouse.ai" \
            --set "PLAYWRIGHT_BASE_URL=https://${QA_INSTANCE_SUBDOMAIN}.longhouse.ai" \
            --set "PLAYWRIGHT_API_BASE_URL=https://${QA_INSTANCE_SUBDOMAIN}.longhouse.ai" \
            --set "LONGHOUSE_JOURNEY_OUTPUT=/work/artifacts/cohort-journey/cohort-journey.json"

      - name: Verify deployed source after QA
        if: always() && steps.runtime_before.outcome == 'success'
        env:
          EXPECTED_RUNTIME_SHA: ${{ steps.runtime_before.outputs.runtime_sha }}
        run: |
          set -euo pipefail
          health="$(curl --fail --silent --show-error --max-time 15 "https://${QA_INSTANCE_SUBDOMAIN}.longhouse.ai/api/health")"
          printf '%s\n' "$health" | jq '{build}'
          printf '%s\n' "$health" | jq --exit-status --arg expected "$EXPECTED_RUNTIME_SHA" '.build.commit == $expected and .build.dirty == false'

      - name: Write summary
        if: always()
        run: |
          {
            echo "## Hosted Live QA"
            echo ""
            echo "- Instance: \`$QA_INSTANCE_SUBDOMAIN\`"
            echo "- Trigger: \`${{ github.event_name }}\`"
            echo "- Commit: \`${{ github.sha }}\`"
            echo "- QA source SHA: \`${{ inputs.source_sha || github.sha }}\`"
            echo "- Requested runtime image: \`${{ inputs.runtime_image_tag || inputs.source_sha || 'latest' }}\`"
            echo "- Observed runtime SHA: \`${{ steps.runtime_before.outputs.runtime_sha || 'unavailable' }}\`"
            echo "- qa-live: \`${{ steps.qa_live.outcome || 'skipped' }}\`"
            echo "- hosted shipper mixed bench: \`${{ steps.hosted_shipper_bench.outcome || 'skipped' }}\`"
            echo "- render-canary: \`${{ steps.render_canary.outcome || 'skipped' }}\`"
            echo "- cohort contracts: \`${{ steps.cohort_contracts.outcome || 'skipped' }}\`"
            echo "- cohort journey: \`${{ steps.cohort_journey.outcome || 'skipped' }}\`"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload privacy-safe cohort journey artifact
        if: always() && steps.cohort_journey.outcome != 'skipped'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: cohort-journey-${{ github.run_id }}-${{ github.run_attempt }}
          path: artifacts/test-isolation/**/files/1/cohort-journey/*.json
          if-no-files-found: error
          retention-days: 30

      - name: Upload live QA artifacts
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: hosted-live-qa-failure-${{ github.run_id }}-${{ github.run_attempt }}
          path: artifacts/test-isolation/**
          if-no-files-found: ignore
          retention-days: 14

      - name: Remove owned QA home
        if: always()
        run: |
          set -euo pipefail
          test "$QA_HOME" = "/tmp/hosted-live-qa-${{ github.run_id }}-${{ github.run_attempt }}"
          rm -rf -- "$QA_HOME"
          test ! -e "$QA_HOME"
