// Lockstep iOS version. Edited by `bump-my-version bump`; see
// .bumpversion.toml at the repo root. Do not hand-edit unless you're
// cutting a release outside the normal flow.
MARKETING_VERSION = 0.1.15
CURRENT_PROJECT_VERSION = 1
