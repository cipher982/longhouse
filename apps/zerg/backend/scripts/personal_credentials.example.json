{
  "_comment": "Personal tool credentials for Jarvis. Copy to personal_credentials.local.json and fill in your values.",

  "traccar": {
    "_comment": "GPS location tracking via Traccar server (uses HTTP Basic Auth)",
    "url": "http://REDACTED_IP:5055",
    "username": "admin",
    "password": "your-traccar-password",
    "device_id": "1"
  },

  "whoop": {
    "_comment": "Health metrics from WHOOP fitness tracker. Register app at https://developer.whoop.com, then do OAuth flow.",
    "client_id": "your-whoop-oauth-client-id",
    "client_secret": "your-whoop-oauth-client-secret",
    "access_token": "your-whoop-oauth-access-token",
    "refresh_token": "optional-refresh-token-for-auto-renewal"
  },

  "obsidian": {
    "_comment": "Search personal notes in Obsidian vault via Runner",
    "vault_path": "~/obsidian_vault",
    "runner_name": "laptop"
  }
}
